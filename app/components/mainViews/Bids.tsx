import { RouteProps } from "@/lib/utils"
import React from "react"
import { Text } from "react-native-paper"

const Bids = ({ route, navigation }: RouteProps) => <Text variant="displayMedium">Bids</Text>

export default Bids